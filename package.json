{
  "name": "softonic-api",
  "version": "0.0.1",
  "type": "module",
  "main": "index.js",
  "license": "ISC",
  "scripts": {
    "apimock": "json-server --watch ./mock/apimock.json --delay 1000",
    "start": "SET NODE_ENV=dev && node index.js",
    "start:dev": "SET NODE_ENV=dev && nodemon index.js",
    "start:prod": "SET NODE_ENV=prod && node index.js",
    "start:linux": "NODE_ENV=dev && node index.js",
    "start:dev:linux": "NODE_ENV=dev && nodemom index.js",
    "start:prod:linux": "NODE_ENV=prod && node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "description": "",
  "dependencies": {
    "body-parser": "^1.19.1",
    "express": "^4.17.2",
    "joi": "^17.6.0",
    "nconf": "^0.11.3",
    "winston": "^3.5.1"
  },
  "devDependencies": {
    "eslint": "^8.8.0",
    "jest": "^27.5.1",
    "json-server": "^0.17.0",
    "nodemon": "^2.0.15"
  }
}
